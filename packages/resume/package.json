{
  "name": "@neelshah400/resume",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "mkdir -p ./dist && typst watch --root ../.. ./src/main.typ ./dist/resume.pdf",
    "build": "mkdir -p ./dist && typst compile --root ../.. ./src/main.typ ./dist/resume.pdf"
  }
}
